export enum NavType {
  COMPONENT = 'component',
  AGENT = 'agent',
  SERVICE = 'service',
}

/**
 * Enum for the different types of Hash Details states
 */
export enum HashDetailsState {
  IS_LOADING = 'IS_LOADING',
  LOADED = 'LOADED',
  FAILED = 'FAILED',
}