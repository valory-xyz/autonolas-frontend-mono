import { BuildAnAgent } from 'components/BuildAnAgent';
import Meta from 'components/Meta';

const BuildPage = () => (
  <>
    <Meta
      title="Build an Agent"
      description="Start building AI agents with Olas using Open Autonomy framework or integrate other agent frameworks with the Olas SDK. Access comprehensive documentation and tools to develop autonomous agents."
      path="build"
    />
    <BuildAnAgent />
  </>
);

export default BuildPage;
