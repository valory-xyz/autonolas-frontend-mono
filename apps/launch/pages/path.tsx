import { Meta } from 'components/Meta';
import { PathPage } from 'components/Path';

const Path = () => (
  <>
    <Meta
      pageTitle="Launcher Paths"
      description="Explore different paths for launching and managing AI agent economies. Choose your approach to deploying autonomous services with Olas Launch."
      pageUrl="path"
    />
    <PathPage />
  </>
);

export default Path;
